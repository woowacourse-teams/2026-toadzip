# Backend Development Cycle

## 1. 작업 계약

| 항목 | 질문 |
|---|---|
| Goal | 사용자가 얻는 결과는 무엇인가? |
| Context | 관련 코드, 테스트, 결정은 어디에 있는가? |
| Constraints | 기술·범위·안전 제한은 무엇인가? |
| Done | 어떤 동작과 명령으로 완료를 증명하는가? |

결과를 바꾸는 모호함만 구현 전에 질문하고, 나머지는 최소 가정으로 진행한다.

## 2. 탐색

1. `docs/README.md`에서 필요한 문서를 고른다.
2. production code와 가까운 test를 함께 읽는다.
3. 진입점부터 Domain과 Adapter까지 호출·데이터 흐름을 추적한다.
4. 공개 계약, 트랜잭션, 외부 연동과 실패 경로를 확인한다.
5. 발견 사실과 추론을 구분해 파일 근거를 남긴다.

## 3. Red → Green → Refactor

| 단계 | 해야 할 일 | 다음 단계 조건 |
|---|---|---|
| Red | 관찰 가능한 행동의 최소 실패 테스트 작성 | 빠진 행동 때문에 실패 |
| Green | 정상·오류 경로를 포함한 최소 구현 | 새 테스트와 관련 테스트 통과 |
| Refactor | 이름, 책임, 중복, 응집도 개선 | 행동 보존 재검증 |

- 컴파일·설정 오류를 Red 증거로 사용하지 않는다.
- 테스트를 맞추는 하드코딩이나 계층 우회를 금지한다.
- 새 행동이 필요하면 별도의 Red 단계로 돌아간다.

## 4. Verify → Review

1. 변경한 테스트 클래스 또는 가장 가까운 test task를 실행한다.
2. 영향 기능의 테스트 묶음을 실행한다.
3. 가능한 경우 `./gradlew check`를 실행한다.
4. 아키텍처, 포맷, 정적 분석, 통합 테스트 결과를 확인한다.
5. diff에서 범위 이탈, 회귀, 보안, 비밀 노출을 검토한다.

## 5. 완료와 학습

- 변경 파일과 사용자 관점의 결과를 요약한다.
- 실행 명령, 결과, 미실행 검사와 이유를 보고한다.
- 같은 실패가 반복되면 문서보다 자동 검사로 규칙을 승격한다.
- 검증하지 못한 항목은 통과로 간주하지 않는다.
