spring.application.name=backend

server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax

app.admin.cors.allowed-origin=${ADMIN_CORS_ALLOWED_ORIGIN:http://localhost:5173}
