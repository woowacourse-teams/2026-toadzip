# PostgreSQL
POSTGRES_DB=toadzip
POSTGRES_USER=toadzip
POSTGRES_PASSWORD=change-me

# Port exposed on the host machine
BACKEND_PORT=8080

# Shared PostgreSQL (optional)
SHARED_DATASOURCE_ENABLED=false
SHARED_DATASOURCE_URL=
SHARED_DATASOURCE_USERNAME=
SHARED_DATASOURCE_PASSWORD=
